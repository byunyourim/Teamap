# Teamap 기능 설계 문서

개발팀 생산성 + 블록체인 서비스 운영을 위한 데스크톱 앱의 기능 구조입니다.

---

## 📐 전체 메뉴 구조

```
- 홈 (대시보드)
- 일정
  - 캘린더
- 업무
  - 이슈
  - PR
  - 주간 리포트
- 운영
  - 에러 로그
  - 서비스 관리
  - 온체인 모니터링
  - 인시던트              🆕
  - 배포 트래킹           🆕
  - Reconciliation         🆕
  - 런북 (SOP)
- 분석
  - 서비스 메트릭
  - 가스비 / 비용 추이     🆕
  - 보안 모니터            🆕
  - 월간 리포트
- 설정
  - 계정 및 권한
  - 알림
  - 일반 (언어/시간대/시작페이지/밀도)
  - 테마 (dark / light / system)
  - API 키 (safeStorage)
  - 단축키
```

---

## 0. 홈 (대시보드)

앱을 켜면 가장 먼저 보이는 화면. 여러 메뉴를 순회하지 않고도 현재 상태를 한눈에 파악하는 것이 목적.

### KPI 카드
한 화면에 핵심 지표를 카드로 배치:
- **미해결 에러** — 오늘/이번 주 발생 건수, 미처리 건수
- **내 PR** — 리뷰 대기 / 머지 대기 / 변경 요청 받은 건수
- **오늘 배치** — 예정/실행 중/실패 건수
- **운영 지갑 잔고** — 멀티체인 잔고 합계 + 임계값 미만이면 경고
- (선택) 미해결 이슈 / 이번 주 일정 / 온콜 담당자

### 빠른 액션
자주 쓰는 동작에 한 번에 접근:
- **배치 실행** 버튼
- **최근 에러로 점프** — 가장 최근/가장 심각한 에러 상세로 바로 이동
- **PR 리뷰하러 가기** — 내가 리뷰어인 PR로 바로 이동
- **Pending Tx 확인** — 막혀 있는 Tx 모음으로 점프

### 위젯 커스터마이즈 (선택)
- 어떤 KPI 카드를 보일지 토글
- 카드 순서 드래그 정렬

---

## 1. 일정

### 1-1. 캘린더
- 팀 일정/회의 표시
- **배포 일정** 표시 (PR 머지 → 배포 예정일)
- **온콜(당직) 일정** 표시 — 이번 주 누가 담당인지 한눈에
- (선택) Google Calendar 양방향 동기화

---

## 2. 업무

### 2-1. 이슈
- GitHub 이슈 목록 / 상세 / 필터
- 라벨/마일스톤/담당자별 그룹핑
- Stale 이슈 자동 추적 알림 (이미 `scheduler.ts` 존재)

### 2-2. PR
- PR 목록 / 상세 / 리뷰 상태
- 변경된 diff 보기
- (확장) AI 요약 — "이 PR을 한 줄로" 버튼
- (확장) 리뷰 코멘트 초안 자동 생성

### 2-3. 주간 리포트
- 본인이 머지한 PR / 클로즈한 이슈 자동 집계
- 마크다운 리포트 자동 생성
- 슬랙 공유 버튼

---

## 3. 운영

### 3-1. 에러 로그
운영 차별화 핵심 영역.

- **Slack 로그 + AI 원인 분석** → 에러 지점 파악
- **txhash 입력 → 이더스캔/아바스캔 조회** → 트랜잭션 정보/실패 사유 디코딩
- **에러 그룹핑** — 같은 스택트레이스끼리 묶어서 "이번 주 47회" 식 집계
- **에러 → GitHub 이슈 자동 변환** 버튼
- **온콜 라우팅** — 심각도 높은 에러는 현재 당직자에게 즉시 푸시
- **타임라인 뷰** — 배포 시각 / 배치 실행 시각과 겹쳐 인과 파악

### 3-2. 서비스 관리
배치 프로그램 운영.

- 현재 배치 프로그램: **실행 / 중단 / 설정 변경**
- **실행 이력** (성공/실패/소요시간) 그래프
- **다음 실행 예정 시간** (cron 파싱)
- **수동 실행 시 확인 다이얼로그** (실수 방지)
- **변경 이력 / 감사 로그** — "누가 언제 설정 바꿨나"
- **헬스체크 LED** — 서비스별 UP/DOWN 한눈에

### 3-3. 온체인 모니터링 🆕
이더스캔 단발 조회를 넘어선 운영용 도구.

- **운영 지갑 잔고 워처** — 가스비 임계값 이하면 슬랙 알림
- **Pending Tx 트래커** — 30분 이상 안 빠지면 경고
- **실패 Tx 모음** — revert 사유까지 디코딩
- **컨트랙트 상태 모니터링** — `paused`, `owner` 변경 등 이벤트 실시간 구독
- **멀티체인 통합 뷰** — Ethereum / Avalanche / Polygon 등 한 화면

### 3-4. 인시던트 관리 🆕
**가장 임팩트 큰 기능.** 장애 발생 시 정보가 슬랙/메모/이슈에 흩어지는 문제 해결.

- **인시던트 생성** — 1클릭 (또는 에러 로그에서 "인시던트 만들기")
- **자동 타임라인** — 에러/배포/배치 실행/액션이 시각순으로 자동 누적
- **상태 관리** — Investigating / Identified / Monitoring / Resolved / Postmortem
- **심각도 (Severity)** — SEV1 / SEV2 / SEV3
- **영향 태깅** — 영향받은 서비스 / 지갑 / 컨트랙트 / 사용자 수
- **런북 자동 연결** — 에러 패턴 매칭으로 관련 SOP 추천
- **회고 (Postmortem)** — 마크다운으로 작성, 같은 종류 인시던트 검색 가능
- **유사 인시던트** — "이거 같은 장애 또 났어?" 자동 검색

### 3-5. 배포 트래킹 🆕
**에러 디버깅 효율 2배.** 에러 발생 시각 = 어느 배포 직후?

- **배포 이력** — PR 머지 → 배포 시각 자동 기록 (CI/CD 웹훅 또는 수동)
- **타임라인 뷰** — 캘린더에 배포 마커 + 에러 로그 시각 겹침
- **컨트랙트 업그레이드 이력** — `Upgraded` / `OwnershipTransferred` 이벤트 자동 추적
- **롤백 버튼** — 이전 배포로 빠른 복구
- **배포 → 인시던트 연결** — 배포 후 1시간 내 인시던트 자동 후보 제안

### 3-6. Reconciliation (데이터 검증) 🆕
**블록체인 운영에 가장 특화된 가치.**

- **온체인 ↔ 내부 DB 일치 검증**
- **누락된 Tx** — 컨트랙트 이벤트 vs DB 처리 기록
- **미처리 입금** — 사용자 잔고 안 올라간 케이스
- **잔고 오차** — 사용자 잔고 합 vs 컨트랙트 잔고
- **자동 일일 검증** — 매일 N시 비교, 불일치만 알림
- **불일치 → 인시던트 자동 생성** (선택)

### 3-7. 런북 (SOP)
장애 대응 매뉴얼 위키.

- "DB 죽으면 어떻게?" 같은 표준 운영 절차
- 마크다운 기반 위키
- 에러 로그 / 인시던트에서 바로 관련 런북으로 점프

---

## 4. 분석

### 4-1. 서비스 메트릭
- 성공률 / 평균 응답시간 / 에러율 추이
- 서비스별 KPI 카드

### 4-2. 가스비 / 비용 추이 🆕
- 일/주/월별 가스비 사용량 (체인별)
- 어느 컨트랙트가 가스 많이 먹나
- RPC API 비용 (Infura/Alchemy 사용량)
- 임계 초과 시 경고

### 4-3. 보안 모니터 🆕
- **의심 거래 탐지**
  - 화이트리스트 외 주소로 출금
  - 큰 금액 단건 (예: 5 ETH 이상)
  - 신규 외부 컨트랙트 호출
- **권한 변경 추적** — `OwnershipTransferred`, `RoleGranted/Revoked`
- **감사 로그 export**

### 4-4. 월간 리포트
- 주간 리포트("내가 한 일")와 구분되는 "서비스가 어떻게 돌았나"

---

## 5. 설정

### 5-1. 계정 및 권한
- **계정 정보** — 이름 / 이메일 / 프로필 사진 / GitHub 토큰
- **권한 관리(Role)** — 운영 도구는 권한 분리 필수
  - `Admin` — 모든 설정 변경, 배치 실행/중단, 권한 부여
  - `Operator` — 배치 실행/중단, 에러 처리
  - `Viewer` — 조회만 가능
- **담당 프로젝트** — 본인이 모니터링할 대상 등록
  - GitHub 레포지토리 선택 (이슈/PR 필터링 기준)
  - 담당 서비스(배치) 매핑 — 알림 라우팅, 홈 대시보드 KPI 필터에 사용
  - 담당 컨트랙트 주소 / 운영 지갑 등록
- **세션 / 로그아웃**

### 5-2. 알림
- **채널별 라우팅** — Slack / 데스크톱 푸시 / Email 중 어디로 보낼지 선택
- **이벤트 종류별 ON/OFF** — 에러 / PR 리뷰 요청 / 멘션 / 배치 실패 / Tx 실패 등
- **우선순위별 라우팅** — 심각도 높음 → 푸시, 낮음 → 슬랙만
- **DND(조용한 시간)** — 시간대 지정 (예: 22:00-08:00 푸시 OFF)
- **온콜 로테이션** — 이번 주 당직자 자동 라우팅
- **알림 소리** — 토글 / 사운드 선택

### 5-3. 일반
- **시간대** — UTC 기반 블록체인 Tx 시각 표시에 중요. Local / UTC / 임의 TZ 선택
- **날짜/시간 형식** — `YYYY-MM-DD` 등
- **시작 페이지** — 앱 실행 시 진입할 메뉴 (홈 / 이슈 / 에러 로그 등)
- **정보 밀도** — Compact / Comfortable (테이블 행 높이)
- **폰트 크기** — 접근성 / 모니터 해상도 대응
- **OS 부팅 시 자동 실행** 여부
- **창 닫기 동작** — 종료 / 트레이로 최소화

### 5-4. 테마
- **Dark / Light / System** — System은 OS 설정 따라가기 (Electron `nativeTheme` 연동)
- **액센트 색상** (선택) — 사이드바 활성 색 등 포인트 컬러 지정
- 실시간 미리보기 — 적용 전 샘플 화면으로 확인

### 5-5. API 키
한 화면에서 외부 통합 키를 관리. **OS keychain(Electron `safeStorage`)에 암호화 저장** — 평문 저장 금지.

| 통합 | 용도 |
| --- | --- |
| GitHub | 이슈/PR 조회, 토큰 권한 표시 |
| Etherscan / Avascan | 온체인 조회 |
| Infura / Alchemy (선택) | RPC 호출 |
| Slack | 알림 송신, 로그 수집 |
| Anthropic / OpenAI (선택) | AI 원인 분석, PR 요약 |

- **연결 테스트 버튼** — 키 저장 직후 유효성 검증
- **만료 알림** — 곧 만료되는 토큰 사전 경고

### 5-6. 단축키
- **기본 단축키 안내** — 한 화면에 표 형태로 정리
- **커스텀 매핑** — 충돌 방지 검사 포함 (파워유저용)
- 추천 기본값:
  - `Cmd+K` — 글로벌 검색
  - `g i` — 이슈, `g p` — PR, `g e` — 에러 로그 (GitHub 식 네비게이션)
  - `Cmd+,` — 설정 열기
  - `Cmd+R` — 현재 페이지 새로고침

---

## 🎁 전역 기능 (메뉴 외)

- **글로벌 검색 `Cmd+K`** — 이슈 / PR / 에러 / Tx 통합 검색
- **트레이 아이콘** — 창을 닫아도 트레이에 남아 알림 수신
- **OS 부팅 시 자동 실행** 옵션
- **자동 업데이트** (`electron-updater`)
- **에러 바운더리** — 한 페이지 에러로 앱 전체 다운 방지

---

## 🛠️ 기술적 보완 (백로그)

- `tsc -b` GitHub Actions CI
- Vitest 실제 테스트 — `scheduler`, `notifications`, `github` 부터
- `electron-builder` 패키징 (`.dmg` / `.exe`)
- GitHub 토큰 → `localStorage` 대신 `safeStorage` (OS keychain)

---

## 우선순위 가이드

| 우선순위 | 항목 | 이유 |
| --- | --- | --- |
| ⭐⭐⭐ | 홈 대시보드 (KPI + 빠른 액션) | 가장 자주 보는 화면, 첫인상 |
| ⭐⭐⭐ | 테마 (dark/light/system) | 데일리 사용 도구라 체감 가장 큼 |
| ⭐⭐⭐ | 에러 바운더리 + Electron 패키징 | 실사용 진입 장벽 |
| ⭐⭐⭐ | 에러 로그 (Slack+AI+txhash) | 핵심 차별화 |
| ⭐⭐⭐ | **인시던트 관리** 🆕 | 가장 큰 일상적 가치, 차별화도 큼 |
| ⭐⭐⭐ | **배포 트래킹** 🆕 | 에러 디버깅 효율 2배 |
| ⭐⭐⭐ | **Reconciliation** 🆕 | 블록체인 특화, 다른 도구에 거의 없음 |
| ⭐⭐ | 서비스 관리 (배치) | 운영 필수 |
| ⭐⭐ | 온체인 모니터링 | 블록체인 서비스 핵심 |
| ⭐⭐ | 가스비 / 비용 추이 🆕 | 즉시 비용 절감 효과 |
| ⭐⭐ | 보안 모니터 🆕 | 사고 예방 |
| ⭐ | 분석 / 런북 / 글로벌 검색 | 시간 여유 시 |
